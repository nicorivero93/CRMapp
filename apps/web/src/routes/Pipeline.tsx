import { ComingSoon } from '@/components/ComingSoon';

export default function Pipeline() {
  return (
    <ComingSoon
      feature="Pipeline"
      phase="L2.5"
      note="Kanban drag & drop de deals por stage. Se porta después de conectar contactos y WhatsApp."
    />
  );
}
