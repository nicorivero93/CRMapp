import { ComingSoon } from '@/components/ComingSoon';

export default function Contacts() {
  return (
    <ComingSoon
      feature="Contactos"
      phase="L2.3-L2.4"
      note="Import CSV, tabla con filtros y timeline del contacto. Los leads y contactos comparten backend."
    />
  );
}
