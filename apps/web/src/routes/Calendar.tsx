import { ComingSoon } from '@/components/ComingSoon';

export default function Calendar() {
  return (
    <ComingSoon
      feature="Calendario"
      phase="Post-MVP"
      note="El calendario no está en el roadmap inicial de la Local Edition. El foco es leads + WhatsApp."
    />
  );
}
